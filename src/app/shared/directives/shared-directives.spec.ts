import { SharedDirectives } from './shared-directives';

describe('SharedDirectives', () => {
  it('should create an instance', () => {
    const directive = new SharedDirectives();
    expect(directive).toBeTruthy();
  });
});
